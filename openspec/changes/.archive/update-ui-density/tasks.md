## 1. Implementation
- [x] 1.1 Update `desktop/src/renderer/src/theme/index.ts` to reduce `shape.borderRadius` to 8
- [x] 1.2 Add `defaultProps` with `size: 'small'` for compact components (MuiButton, MuiIconButton, MuiTextField, MuiSelect, MuiChip, MuiTable)
- [x] 1.3 Add `MuiToolbar` with `variant: 'dense'` for compact height
- [x] 1.4 Add `MuiList` and `MuiMenuItem` with `dense: true`
- [x] 1.5 Reduce `MuiCard` border radius to 8
- [x] 1.6 Reduce `MuiButton` border radius to 16

## 2. Testing
- [x] 2.1 Run `pnpm build` in desktop to verify no TypeScript errors
- [ ] 2.2 Visual verification of compact UI (manual)

## 3. Documentation
- [x] 3.1 No UI guidelines to update (confirmed)
